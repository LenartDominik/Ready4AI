# NOTE: `perplexity` may not be available on PyPI — remove or replace if pip fails
# perplexity>=0.1.0
python-dotenv>=1.0.0
requests>=2.28.0
# Opcjonalnie, jeśli używasz backendu Flask / LangChain:
flask>=2.2.0
flask-cors>=3.0.10
langchain>=0.3.0
pydantic>=1.10.0
